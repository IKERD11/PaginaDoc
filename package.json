{
  "name": "paginadoc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node node_modules/esbuild/bin/esbuild firebase-bundle-src.js --bundle --minify --outfile=js/firebase-bundle.js --platform=browser",
    "start": "npx serve ."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.95.3",
    "firebase": "^12.9.0",
    "firebase-admin": "^13.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}