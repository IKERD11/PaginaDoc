{
  "scripts": {
    "build": "npx esbuild firebase-bundle-src.js --bundle --outfile=js/firebase-bundle.js --platform=browser",
    "start": "npx serve ."
  },
  "dependencies": {
    "firebase": "^12.9.0",
    "firebase-admin": "^13.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}